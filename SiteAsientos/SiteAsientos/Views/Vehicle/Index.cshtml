@model IEnumerable<SiteAsientos.Models.Vehicle>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle_Brand)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle_Model)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vehicle_ModelYear)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Vehicle_Brand)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vehicle_Model)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vehicle_ModelYear)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Vehicle_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Vehicle_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Vehicle_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
