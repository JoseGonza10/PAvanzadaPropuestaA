@model SiteAsientos.Models.CentralDesign

@{
    ViewData["Title"] = "Edit";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Edit</h1>

<h4>CentralDesign</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CentralDesign_Id" />
            <div class="form-group">
                <label asp-for="CentralDesign_MaterialId" class="control-label"></label>
                <select asp-for="CentralDesign_MaterialId" class="form-control" asp-items="ViewBag.CentralDesign_MaterialId"></select>
                <span asp-validation-for="CentralDesign_MaterialId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="CentralDesign_ColorId" class="control-label"></label>
                <select asp-for="CentralDesign_ColorId" class="form-control" asp-items="ViewBag.CentralDesign_ColorId"></select>
                <span asp-validation-for="CentralDesign_ColorId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
