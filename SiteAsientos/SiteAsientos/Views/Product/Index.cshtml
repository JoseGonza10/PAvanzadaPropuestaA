@model IEnumerable<SiteAsientos.Models.Product>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout2.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Product_Embroidery)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Order)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Design)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supplier)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Product_Embroidery)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Order.Order_Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Design.Design_Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supplier.Supplier_Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Product_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Product_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Product_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
